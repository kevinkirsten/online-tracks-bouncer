# Online Tracks Bouncer

Free online multitrack audio mixer. Bounce stems, merge WAV/MP3 files, and export your mix directly in the browser.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2Run the app:
   `npm run dev`
